


//When use like this.It Will take the entire HTML Element..So the It return an Error
//We Need to mention the HTML element  using keyword : as
const element=document.getElementById('myElement') as HTMLInputElement;

element.value="Hello World";

